<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta http-equiv="X-UA-Compatible" content="IE=edge">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Document</title>
    <link rel="stylesheet" href="css/style.css">
</head>
<body>
    <h1>Todos</h1>
    <ul>
    <% for(let i = 0; i < zebra.length; i++) { %> <!--loops through the key (created on line 28 in server.js) and returns a document from the database -->
        <li class='todoItem'>
            <% if(zebra[i].completed === true) {%> <!--if the document has a key value pair of completed: true... -->
            <span class="completed"><%= zebra[i].todo %></span> <!--...return a span with a class of 'completed' -->
            <% }else{ %>
            <span><%= zebra[i].todo %></span> <!-- ...else return the document -->
            <% } %>   
            <span class='del'> Delete </span>
        </li>
    <% } %>    
    </ul>

    <h2>Things left to do: <%= left %></h2>

    <form action="/createTodo" method='POST'>
        <input type="text" placeholder="Enter Todo Item" name='todoItem'>
        <input type="submit">
    </form>

    <script src="js/main.js"></script>
</body>
</html>
<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta http-equiv="X-UA-Compatible" content="IE=edge">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Document</title>
    <link rel="stylesheet" href="css/style.css">
</head>
<body>
    <h1>Todos</h1>
    <ul>
<% // start a for loop, repeat the loop for all of "zebra", incrementing by one each time %>
        <% for(let i = 0; i < zebra.length; i++) { %>
        <li class='todoItem'>
<% // if this item has been completed %>
            <% if(zebra[i].completed === true) {%>
<% // then output the todo, with a class "completed" which probably has a strikethrough in CSS %>
                <span class="completed"><%= zebra[i].todo %></span>
<% // else output the todo, with no class %>
            <% }else{ %>
            <span><%= zebra[i].todo %></span>
            <% } %>   
            <span class='del'> Delete </span>
        </li>
<% // end loop %>
        <% } %>    
    </ul>

<% // output the variable "left" which is the number of todos not marked complete %>
    <h2>Things left to do: <%= left %></h2>

    <form action="/createTodo" method='POST'>
        <input type="text" placeholder="Enter Todo Item" name='todoItem'>
        <input type="submit">
    </form>

    <script src="js/main.js"></script>
</body>
</html>
<!DOCTYPE html>
<html lang="en">
  <head>
    <meta charset="UTF-8" />
    <meta http-equiv="X-UA-Compatible" content="IE=edge" />
    <meta name="viewport" content="width=device-width, initial-scale=1.0" />
    <title>Document</title>
    <link rel="stylesheet" href="css/style.css" />
  </head>
  <body>
    <h1>Todos</h1>
    <ul>
      <!--This for loop iterates over the array pulled from the db-->
      <% for(let i = 0; i < zebra.length; i++) { %>
      <li class="todoItem">
        <!--This if/else statement looks at each item in the above loop and decides whether to assign the class completed-->
        <% if(zebra[i].completed === true) {%>
        <span class="completed"><%= zebra[i].todo %></span>
        <% }else{ %>
        <span><%= zebra[i].todo %></span>
        <% } %>
        <span class="del"> Delete </span>
      </li>
      <% } %>
    </ul>

    <h2>Things left to do: <%= left %></h2>
    <!-- this form is where POST requests are submitted to the db to add new todo items -->
    <form action="/createTodo" method="POST">
      <input type="text" placeholder="Enter Todo Item" name="todoItem" />
      <input type="submit" />
    </form>

    <script src="js/main.js"></script>
  </body>
</html>

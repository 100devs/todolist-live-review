<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta http-equiv="X-UA-Compatible" content="IE=edge">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Document</title>
    <link rel="stylesheet" href="css/style.css">
</head>
<body>
    // Our heading to give a headline to our app
    <h1>Todos</h1>
    <ul>
    <!-- Opening our for loop code block  -->
    <% for(let i = 0; i < zebra.length; i++) { %>
        <li class='todoItem'>
            <% if(zebra[i].completed === true) {%>
            <span class="completed"><%= zebra[i].todo %></span>
            <% }else{ %>
            <span><%= zebra[i].todo %></span>
            <% } %>   
            <span class='del'> Delete </span>
        </li>
    <!-- Closing our for loop code block         -->
    <% } %>    
    </ul>

    <h2>Things left to do: <%= left %></h2>

    // Our simple form to give users an area to input their todo task; additionally we have the method set to 'POST' so we can see that ping in our server
    <form action="/createTodo" method='POST'>
        // Input element with a name that we'll be referring to when we want to grab the data from the form the user enters
        <input type="text" placeholder="Enter Todo Item" name='todoItem'>
        // Input to allow our user to submit the data to the todo list
        <input type="submit">
    </form>

    <script src="js/main.js"></script>
</body>
</html>
<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta http-equiv="X-UA-Compatible" content="IE=edge">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Document</title>
    <link rel="stylesheet" href="css/style.css">
</head>
<body>
    <h1>Todos</h1>
    <ul>
    <% for(let i = 0; i < zebra.length; i++) { %>              //loops through zebra array DB 
        <li class='todoItem'>                                  //Creates list item for looped item
            <% if(zebra[i].completed === true) {%>             //Displays span with strikethrough if class equals completed
            <span class="completed"><%= zebra[i].todo %></span>
            <% }else{ %>
            <span><%= zebra[i].todo %></span>                  //Displays span of to-do item uncompleted
            <% } %>   
            <span class='del'> Delete </span>                  //Adds delete item functionality displayed as span
        </li>
    <% } %>    
    </ul>

    <h2>Things left to do: <%= left %></h2>                    //Shows item count to-do

    <form action="/createTodo" method='POST'>
        <input type="text" placeholder="Enter Todo Item" name='todoItem'>
        <input type="submit">
    </form>

    <script src="js/main.js"></script>
</body>
</html>